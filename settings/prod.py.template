from settings.defaults import *

DEBUG = False

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': '{PG_DATABASE}',
        'USER': '{PG_USER}',
        'PASSWORD': '{PG_PASSWORD}',
        'HOST': '{PG_HOST}',
        'PORT': '{PG_PORT}',
    }
}

